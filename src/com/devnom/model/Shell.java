package com.devnom.model;

public class Shell {
    String type; // I have more than a binary option here
                 // Possibly use an enum?
                 // Same design decision as the frame
}
